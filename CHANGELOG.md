# Changelog

## Unreleased

- Added synced Studio-ready `.model.json` assets under `test/SyncedModels` so models appear immediately after Rojo sync.
- Added a static `SkyIslandHub` world model based on the PNG-inspired sky-island layout.
- Kept the runtime world generator in `server/WorldBuilder.lua` for live gameplay builds during Play mode.
- Updated the Studio helper scripts to place synced models into `Workspace` first and fall back to runtime-generated samples when needed.
- Added creator-group metadata support for `RN_Gaming`.
- Added generated-project `INSTALL_AND_USAGE.md` documentation.
- Expanded the root README with recent changes and updated Rojo install instructions.
